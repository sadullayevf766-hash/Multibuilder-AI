bash

grep -n "6\. Fixtures\|Fixture labels\|renderFixtureLabel\|renderFixture}" /home/claude/project/client/src/components/Canvas2D.tsx | head -10
Output

144:  const renderFixtureLabel = (fixture: PlacedFixture) => {
504:          {/* 6. Fixtures (covers drain pipe start) */}